# Passed to docker compose build and up.
# UID is readonly in bash so it cannot be overridden on the command line.
# Docker Compose reads these from this file automatically — no export needed.
DOCKER_UID=1000
DOCKER_GID=1000
